# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Preferred notification email delivery: Brevo Transactional Email
BREVO_API_KEY=xkeysib_replace_with_your_brevo_api_key
BREVO_SENDER_EMAIL=verified-sender@example.com
BREVO_SENDER_NAME=Sahil Portfolio

# Optional SMTP fallback if Brevo is not configured
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
FROM_EMAIL="Sahil Portfolio <no-reply@example.com>"

# Optional: where new booking/UPI submission notifications are sent.
BOOKING_NOTIFY_EMAIL=minersahil20@gmail.com

# Direct UPI checkout is embedded in server.js, so these values are not
# required in Vercel environment variables. Update EMBEDDED_CHECKOUT_CONFIG
# in server.js if the payee UPI ID or booking email changes.
